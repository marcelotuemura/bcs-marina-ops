import DashboardShell from '@/components/dashboard-shell'
export default function SlipsPage() { const slips = Array.from({ length: 16 }, (_, i) => `Slip ${String.fromCharCode(65 + Math.floor(i/4))}-${i%4+1}`); return <DashboardShell eyebrow="Occupancy" title="Slips & Storage" subtitle="Visual slip map for wet slips, dry storage, reservations, occupancy, and billing."><div className="card"><div className="calendar-grid">{slips.map((s,i) => <div className="day" key={s}><strong>{s}</strong><div className="event">{i % 5 === 0 ? 'Available' : 'Occupied'}</div></div>)}</div></div></DashboardShell> }
