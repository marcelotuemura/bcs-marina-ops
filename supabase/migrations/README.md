# Supabase migrations

For this synchronized release, run only:

`000_enterprise_v2_synchronized_schema.sql`

Do not run the older prototype migration files unless you are intentionally studying the previous drafts.
