# BCS Marina Ops

Starter Next.js app for marina workflow, estimates, invoices, projects, Supabase auth, and Stripe checkout.

## Setup

1. Copy `.env.local.example` to `.env.local`
2. Add your real Supabase and Stripe keys
3. Run `npm install`
4. Run `npm run dev`
5. Deploy to Vercel
